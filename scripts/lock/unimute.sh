#!/bin/bash

chmod -i /etc/sudoers

chmod -i /etc/security/*

chmod -i /etc/passwd

chmod -i /etc/shadow

chmod -i /etc/hosts

chmod -i /etc/hosts.allow

chmod -i /etc/hosts.deny

chmod -i /var/spool/cron/*
